services:
  backend:
    build: ./backend
    container_name: carbon_backend
    ports:
      - "5000:5000"
    environment:
      PORT: 5000
      DB_USER: shriya.banoth5607gmail.com
      DB_HOST: host.docker.internal
      DB_NAME: carbon_emissions_db
      DB_PORT: 5432

  frontend:
    build: ./frontend
    container_name: carbon_frontend
    ports:
      - "5173:5173"
    depends_on:
      - backend
